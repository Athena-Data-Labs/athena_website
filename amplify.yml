version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm@9.12.3
        - rm -rf node_modules
        - pnpm install --no-frozen-lockfile
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - /root/.local/share/pnpm/store/**/*
